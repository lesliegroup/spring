<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itleslie.cache.mapper.DepartmentMapper">
    <select id="selectById" resultType="department">
        select * from department where id = #{id}
    </select>

    <update id="updateDep" parameterType="department" >
        update department set departmentName=#{departmentName}  where id = #{id}
    </update>

    <select id="getDepByDepartmentName" resultType="department">
        select * from department where departmentName = #{departmentName}
    </select>
</mapper>